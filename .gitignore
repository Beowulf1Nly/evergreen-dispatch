# Never commit these. The whole point of this repo is that the public
# copy contains nothing but ciphertext.
private/
*.local
node_modules/

# wrangler caches your account id and email here — never publish it
worker/.wrangler/
.wrangler/

# stray HQ exports — plaintext customer data, never publish
lawn.txt
clean.txt
*.export.txt
